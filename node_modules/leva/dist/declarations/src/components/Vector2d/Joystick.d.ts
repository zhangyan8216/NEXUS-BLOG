import React from 'react';
import type { Vector2d } from '../../types';
import type { Vector2dProps } from './vector2d-types';
declare type JoystickProps = {
    value: Vector2d;
} & Pick<Vector2dProps, 'onUpdate' | 'settings'>;
export declare function Joystick({ value, settings, onUpdate }: JoystickProps): React.JSX.Element;
export {};
